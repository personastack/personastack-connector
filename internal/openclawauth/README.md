# OpenClaw Auth

This package resolves local OpenClaw Gateway operator credentials for the
Connector. It owns discovery from Connector state, explicit CLI values,
OpenClaw-owned environment/config files, and OpenClaw device auth state.
