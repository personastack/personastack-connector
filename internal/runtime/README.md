# runtime

`runtime` defines Connector-local adapter enums, state enums, and interfaces for
Hermes/OpenClaw runtime implementations.

This package owns typed placeholders only. Real Hermes and OpenClaw networking
must be added behind the adapter interface.
