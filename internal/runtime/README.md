# runtime

`runtime` defines Connector-local adapter enums, state enums, and interfaces for
Hermes/OpenClaw runtime implementations.

This package owns Hermes and OpenClaw runtime detection, dispatch, streaming,
polling, cancellation, and runtime-specific health behavior behind the adapter
interface.
